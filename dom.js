const amount = document.getElementById("amount");

console.log(amount);

const steps = document.getElementsByClassName("step");

// console.log(steps);

const circles = document.getElementsByClassName("circle");

// console.log(circles);

const element = document.querySelector(".receiver > div > div");

console.log(element);

const elements = document.querySelectorAll("form > div");

console.log(elements);

// const a = document.getElementsByTagName("a")

const receivers = document.getElementsByClassName("receiver");

console.log(receivers);

const receiverImg = receivers[0].querySelectorAll("img");
const allImg = document.querySelectorAll("img");

console.log(receiverImg);
console.log(allImg);
